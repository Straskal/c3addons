"use strict";
{
  const C3 = self.C3;
  C3.Plugins.Straskal_Renderbug = class RenderbugPlugin extends (
    C3.SDKPluginBase
  ) {
    constructor(opts) {
      super(opts);
    }

    Release() {
      super.Release();
    }
  };
}
