"use strict";
{
    const BEHAVIOR_CLASS = SDK.Behaviors.Straskal_FamilyContainer;

    BEHAVIOR_CLASS.Type = class FamilyContainerType extends SDK.IBehaviorTypeBase
    {
        constructor(sdkPlugin, iBehaviorType)
        {
            super(sdkPlugin, iBehaviorType);
        }
    };
}